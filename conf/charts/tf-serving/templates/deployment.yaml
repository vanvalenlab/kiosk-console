apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.deployment_name }}
  labels:
    app: {{ .Values.app_name }}
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.app_name }}
  template:
    metadata:
      labels:
        app: {{ .Values.app_name }}
    spec:
      restartPolicy: "Always"
      nodeSelector:
        beta.kubernetes.io/instance-type: "p2.xlarge"
      tolerations:
      - key: "nvidia.com/gpu"
        effect: "NoSchedule"
      containers:
      - name: {{ .Values.container_name }}
        image: {{ .Values.image_name }}:{{ .Values.image_version }}
        imagePullPolicy: "Always"
        resources:
          limits:
            nvidia.com/gpu: 1
        ports:
        - containerPort: {{ .Values.deployment_port }}
        env:
        - name: RPC_PORT
          value: {{ .Values.env.RPC_PORT | quote }}
        - name: REST_PORT
          value: {{ .Values.env.REST_PORT | quote }}
        - name: REST_TIMEOUT
          value: {{ .Values.env.REST_TIMEOUT | quote }}
        - name: MODEL_PREFIX
          value: {{ .Values.env.MODEL_PREFIX }}
        - name: LOG_LEVEL
          value: {{ .Values.env.LOG_LEVEL | quote }}
{{- range $name, $value := .Values.secrets }}
{{- if not ( empty $value) }}
        - name: {{ $name }}
          valueFrom:
            secretKeyRef:
              name: tf-serving-env-variables
              key: {{ $name | lower }}
{{- end }}
{{- end }}
