helmDefaults:
  args:
    - "--wait"
    - "--timeout=600"
    - "--force"
    - "--reset-values"

releases:

################################################################################
## Redis-Janitor ###############################################################
################################################################################

#
# References:
#   - [web address of Helm chart's YAML file]
#
- name: "redis-janitor"
  namespace: "deepcell"
  labels:
    chart: "redis-janitor"
    component: "redis-janitor"
    namespace: "deepcell"
    vendor: "vanvalenlab"
    default: "true"
  chart: '{{ env "CHARTS_PATH" | default "/conf/charts" }}/redis-janitor'
  version: "0.1.0"
  values:
    - app_name: "redis-janitor"
      deployment_name: "redis-janitor-deployment"
      replicas: 1
      image_name: "vanvalenlab/kiosk-redis-janitor"
      image_version: "0.1"
      container_name: "redis-janitor-container"
      service_account_name: "redis-janitor-service-account"
      resources:
        requests:
          cpu: 100m
          memory: 32Mi
        # limits:
        #   cpu: 100m
        #   memory: 64Mi
      env:
        DEBUG: "true"
        INTERVAL: "5"
        REDIS_HOST: "redis-master"
        REDIS_PORT: "6379"
